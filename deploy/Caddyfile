{$APP_DOMAIN} {
    encode zstd gzip

    @backend path /api/* /uploads/* /preset-assets/* /docs* /redoc* /openapi.json /health
    handle @backend {
        reverse_proxy backend:8000
    }

    handle {
        reverse_proxy frontend:80
    }
}
